[package]
name = "gbaroll-signaling"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[dependencies]
prost.workspace = true

[build-dependencies]
prost-build = "0.14"
# Pure-Rust protobuf compiler: no system `protoc` needed.
protox = "0.9"
